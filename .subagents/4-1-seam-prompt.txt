You are a subagent working inside the repository C:/Users/mchay/cloudflare-os-starter. Your job is ONE isolated task. Do not commit, do not run pnpm install, do not modify any file other than the one listed under OUTPUT. If a command fails, note it and continue.

TASK: Author docs/task-runtime-integration.md — the upstream integration-seam verification for the durable task runtime (plan Task 4.1).

Read these pinned-kernel files first (all under C:/Users/mchay/cloudflare-os-starter/cloudflare-os/):
- packages/workshop-backend/src/agent-spawner-binding.d.ts (the redesigned spawnCallable: SpawnCallableOptions, CallableAgent, persistence semantics)
- packages/workshop-backend/src/agent.ts — search for "executeCode", "restore", "spawnCallable" usage and the CODE_MODE_HARNESS
- packages/workshop-backend/src/overseer.ts — search for: gadget facets (rootNameFor/defaultGadgetId), hook registration/restoration (BoundHookRecord, ctx.restore), per-chat agent callbacks (pendingAgentCallbacks), approval queue wiring (applyAction)
- packages/gatekeeper-scheduler/src/types.d.ts (runAt/every/calendarAt/list, ScheduledTaskHook, the restore semantics — valid inside the Gadget itself)
- plans/spawner-with-persistence.md (upstream design: durable calls, persistent stubs)
- docs/code-mode-operator-guide.md (already written; keep terminology consistent)

Then write docs/task-runtime-integration.md with EXACTLY these sections:
1. "The seam" — how a trusted coordinator gadget (a WorkerEntrypoint-bundled gadget) registers a persistent restored hook through the Scheduler session, persists the registration id, and receives delivery after restart. Cite exact symbol names and file paths from the kernel source.
2. "Durable agent calls" — what the new spawnCallable(title, {types, mainType}) contract gives us: typed interface declarations, calls resolving once durably queued, persistent callback stubs passable as call arguments (ctx.restore()), spawned agents persisting across restarts. Include the exact TypeScript shape of CallableAgent and SpawnCallableOptions as declared in the .d.ts.
3. "What the child sees" — the governed callable agent profile: the child's env carries task-facade bindings (never provider credentials); per upstream, spawned-agent env allowlists are temporarily dropped (multi-gadget plan) — state that authority therefore comes from the typed facade, and cite where the env is built (getEnvForLoader) in overseer.ts.
4. "Scheduler delivery semantics" — enablement requirements, list() behavior before enablement, cancellation through the Connections/hook controller; what does NOT exist (no session cancel()).
5. "Open risks" — anything the seam does NOT yet prove for P4/P5 (e.g., whether a coordinator gadget can call spawnCallable at all from a hook callback; whether CallableAgent stubs survive DO restart when stored — cite the design doc's claims vs the .d.ts).

Rules: cite real symbol names you verified by reading the source (quote line-ish context, not line numbers that will drift). Under 250 lines. Plain factual prose, no marketing. If you find a seam gap (something P4 needs that upstream does not provide), list it under Open risks as "GAP:".

OUTPUT FILE: C:/Users/mchay/cloudflare-os-starter/docs/task-runtime-integration.md — create only this file.
When done, print one line: DONE docs/task-runtime-integration.md <number of GAP items found>.
